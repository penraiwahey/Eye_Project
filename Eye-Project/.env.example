# Used by docker-compose.yml to provision the MySQL container.
# Copy to .env and fill in real values; these must match backend/.env.
DB_NAME=admin_panel
DB_USER=admin_panel
DB_PASSWORD=change_me_password
DB_ROOT_PASSWORD=change_me_root_password
